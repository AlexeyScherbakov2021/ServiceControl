<Window x:Class="ServiceControl.View.DumpRegWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceControl.View"
        xmlns:vm="clr-namespace:ServiceControl.ViewModel"
        mc:Ignorable="d"
        Title="Все регистры устройства"  Height="800" Width="1000" WindowStartupLocation="CenterScreen">
    <!--<Window.DataContext>
        <vm:DumpRegWindowViewModel/>
    </Window.DataContext>-->
    <Grid>
        <DataGrid ItemsSource="{Binding listRegister}" AutoGenerateColumns="False" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding CodeFunc}"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}"/>
                <DataGridTextColumn Header="Адрес HEX" Binding="{Binding Address,StringFormat=X2}"/>
                <DataGridTextColumn Header="Значение" Binding="{Binding ValueString}"/>
                <DataGridTextColumn Header="Размер" Binding="{Binding Size}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
