<Window x:Class="ServiceControl.View.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceControl.View"
        xmlns:con="clr-namespace:ConsoleControl.WPF;assembly=ConsoleControl.WPF"
        xmlns:vm="clr-namespace:ServiceControl.ViewModel"
        mc:Ignorable="d"
        Title="{DynamicResource LogPacket}" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:LogWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <ToolBarPanel DockPanel.Dock="Top">
            <CheckBox Content="{DynamicResource Pause}" Margin="10,4" IsChecked="{Binding IsPause}"/>
        </ToolBarPanel>
        <!--<ListBox ItemsSource="{Binding ListString}" Background="Black" Name="lb" Foreground="White" Height="258">
            
        </ListBox>-->
        <Border>
            <ContentPresenter Content="{Binding conCtrl}"/>
        </Border>
    </DockPanel>
</Window>
